---
имя: Новый герой
уровень: 1
очки_способностей: 3
сила: 10
ловкость: 10
интеллект: 10
хп_текущее: 40
мп_текущее: 30
способности_выбранные:
снаряжение_выбранное:
  - Обычная одежда
инвентарь: ""
заметки: ""
Броня: 2
броня: 0
очки_способностей_всего: 3
---

# Атрибуты

| Атрибут | Значение | Модификатор |
|---------|----------|-------------|
|  Сила | `INPUT[number:сила]` | `= round((this.сила - 10)/2)` |
|  Ловкость | `INPUT[number:ловкость]` | `= round((this.ловкость - 10)/2)` |
|  Интеллект | `INPUT[number:интеллект]` | `= round((this.интеллект - 10)/2)` |

# Ресурсы

```dataviewjs
const maxHp = 20 + dv.current().сила * 2;
dv.paragraph(`\`\`\`meta-bind
INPUT[progressBar(title(❤️ Текущее HP), minValue(0), maxValue(${maxHp})):хп_текущее]
\`\`\``);
```

```dataviewjs
const maxMp = 10 + dv.current().интеллект * 2;
dv.paragraph(`\`\`\`meta-bind
INPUT[progressBar(title(💙 Текущее MP), minValue(0), maxValue(${maxMp})):мп_текущее]
\`\`\``);
```


| Бонус брони | Итоговое КБ |
|-------------|-------------|
| `INPUT[number:броня]` | `VIEW[10 + {броня} + (({ловкость}-10)/2)]` |

 **ОС:** `INPUT[number:очки_способностей_всего]`

# Снаряжение

```dataview
TABLE WITHOUT ID
  file.link as Оружие,
  урон as Урон,
  свойства as Свойства
FROM "Снаряжение"
WHERE тип = "Оружие" AND contains(this.снаряжение_выбранное, file.name)
```

```dataview
TABLE WITHOUT ID
  file.link as Броня,
  бонус_брони as "Бонус КБ",
  свойства as Свойства
FROM "Снаряжение"
WHERE тип = "Броня" AND contains(this.снаряжение_выбранное, file.name)
```

# Способности

```dataview
TABLE WITHOUT ID
  file.link as Способность,
  тип as Тип,
  стоимость_ос as "ОС",
  мана as "MP"
FROM "Способности"
WHERE contains(this.способности_выбранные, file.name)
SORT уровень ASC
```

# Инвентарь

`INPUT[textArea:инвентарь]`

# Заметки
`INPUT[textArea:заметки]`